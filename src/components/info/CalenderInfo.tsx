import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';

export const CalenderInfo = () => {
    return (
      <View>
        <Text>CalenderInfo</Text>
      </View>
    );
  }